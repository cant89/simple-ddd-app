import React, { Fragment } from 'react';

import { CustomersList } from '#/domains/customers';

const Home = () => {
  return (
    <Fragment>
      <CustomersList />
    </Fragment>
  );
};

export default Home;
